pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
        //maven { url "https://verve.jfrog.io/ui/native/verve-gradle-release" }
        jcenter()
        flatDir {
            dirs 'common-aar'
        }
    }
}
rootProject.name = "RapidOcrAndroidOnnx"
include ':app'
include ':OcrLibrary'
//include ':appjava'
